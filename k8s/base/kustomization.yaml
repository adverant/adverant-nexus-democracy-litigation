apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nexus

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pvc.yaml

# Note: secret.yaml should be created manually from secret.yaml.example
# and NOT committed to git

labels:
  - pairs:
      app.kubernetes.io/name: democracy-litigation-api
      app.kubernetes.io/component: backend
      app.kubernetes.io/part-of: nexus-platform
