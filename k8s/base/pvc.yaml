apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: democracy-litigation-uploads
  namespace: nexus
  labels:
    app: democracy-litigation-api
    component: storage
    plugin: democracy-litigation
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-path
